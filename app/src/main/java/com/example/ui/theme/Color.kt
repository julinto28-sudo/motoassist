package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Bento Grid Palette Colors
val PrimaryTeal = Color(0xFF006782)       // Bento Deep Cyan/Teal
val SecondaryTeal = Color(0xFF0061A4)     // Bento Accent Blue
val BackgroundLight = Color(0xFFF7F9FC)   // Bento Calm Cool Blue-White Background
val SurfaceLight = Color(0xFFFFFFFF)      // Crisp white card surface
val TextPrimaryLight = Color(0xFF1A1C1E)  // Bento neutral dark text
val TextSecondaryLight = Color(0xFF44474E)// Bento grayish secondary text

val PrimaryDark = Color(0xFF006782)       // Let's keep a consistent brand primary
val SecondaryDark = Color(0xFF0061A4)
val BackgroundDark = Color(0xFF111316)     // Quiet dark bento background
val SurfaceDark = Color(0xFF1A1C1E)        // Neutral dark surface for bento cards
val TextPrimaryDark = Color(0xFFE2E2E6)   // Clear light grayish text
val TextSecondaryDark = Color(0xFF8C9199) // Secondary dark gray text

// Bento Containers Colors
val BentoBlueSkyBg = Color(0xFFD1E4FF)     // Sky Blue Bento Container
val BentoBlueSkyText = Color(0xFF001D36)

val BentoBlueSlateBg = Color(0xFFE2E7FF)   // Blue-Slate / Lavender Bento Container
val BentoBlueSlateText = Color(0xFF141B3D)
val BentoBlueSlateIconBg = Color(0xFF4259A7)

val BentoGreenBg = Color(0xFFD7E8DE)       // Soft Sage Green Bento Container
val BentoGreenText = Color(0xFF111F16)
val BentoGreenIconBg = Color(0xFF3D664D)

val BentoYellowBg = Color(0xFFFDF2D4)      // Warm Yellow / Alert Bento Container
val BentoYellowText = Color(0xFF201C00)
val BentoYellowIconText = Color(0xFF705D00)

val BentoBorderColor = Color(0xFFDDE2EA)   // Bento Default Card Border Color

// Status indicators (Indonesian standards)
val OkGreen = Color(0xFF3D664D)            // Emerald green matching sage green theme
val WarningAmber = Color(0xFF705D00)       // Muted gold yellow matching bento yellow
val CriticalRed = Color(0xFFBA1A1A)        // Refined deep bento red for alerts
val CalmBlue = Color(0xFF0061A4)           // Electric blue for generic actions


